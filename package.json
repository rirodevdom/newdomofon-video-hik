{
  "name": "newdomofon-video-hik",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "python3 scripts/patch-live-segment-storage.py --project-dir . && python3 scripts/patch-fast-media-startup.py --project-dir . && python3 scripts/patch-media-readiness.py --project-dir . && python3 scripts/patch-archive-program-date-time.py --project-dir . && python3 scripts/patch-device-archive-utc.py --project-dir . && python3 scripts/patch-device-archive-candidate-order.py --project-dir . && python3 scripts/patch-archive-session-resilience.py --project-dir . && python3 scripts/patch-native-sdk-media-runtime.py --project-dir . && python3 scripts/patch-native-archive-browser-compat.py --project-dir . && python3 scripts/patch-retired-archive-session-grace.py --project-dir .",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --test --import tsx test/**/*.test.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "express": "^4.21.2",
    "fast-xml-parser": "^5.8.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
